import { LoaderContext } from 'webpack';

interface InjectDynamicCssLoaderContext extends LoaderContext<any> {
    getOptions: () => {
        manifestPath: string;
        cssImportPaths: Array<{
            feature: string;
            scriptPath: string;
            cssImports: string[];
        }>;
    };
}
declare function export_default(this: InjectDynamicCssLoaderContext, source: string): string;

export { export_default as default };

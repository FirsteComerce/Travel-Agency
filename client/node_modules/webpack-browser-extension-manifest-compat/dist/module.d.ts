import { Compiler } from 'webpack';

interface ManifestCompatInterface {
    manifestPath: string;
    browser?: string;
}

declare class ManifestCompatPlugin {
    private readonly options;
    constructor(options: ManifestCompatInterface);
    apply(compiler: Compiler): void;
}

export { ManifestCompatPlugin as default };
